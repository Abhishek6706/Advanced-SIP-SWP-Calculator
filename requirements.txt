# Runtime dependencies (installed into the Vercel serverless bundle).
# Keep this lean — Vercel provides the ASGI server, so uvicorn is not needed
# in production. Dev/test tools live in requirements-dev.txt.
fastapi==0.111.0

# Request/response validation
pydantic==2.7.4
